<% @posts.each do |post| %>
<em><%= link_to post.title, post %></em> <%= link_to "[EDIT]", edit_post_path(post) %>
  <%= link_to '[DESTROY]', post_path(post), method: :delete, data: { confirm: 'Are you sure?' } %> 
</br><%= post.body %></br>
<% end %>
